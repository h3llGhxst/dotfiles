# hypridle — no idle actions; only lid-close lock and resume handling

general {
    lock_cmd = pidof hyprlock || hyprlock       # avoid stacking lockers
    before_sleep_cmd = loginctl lock-session     # lock before suspend
    after_sleep_cmd = hyprctl dispatch dpms on   # wake screen on resume
}

# Dim on idle: disabled deliberately.
# listener {
#     timeout = 270
#     on-timeout = brightnessctl -s set 10%
#     on-resume = brightnessctl -r
# }

# Auto-lock on idle: disabled deliberately.
# listener {
#     timeout = 300
#     on-timeout = loginctl lock-session
# }

# Screen off on idle: disabled deliberately.
# listener {
#     timeout = 360
#     on-timeout = hyprctl dispatch dpms off
#     on-resume = hyprctl dispatch dpms on
# }

# Suspend on idle: disabled deliberately — never sleep on inactivity.
# listener {
#     timeout = 900
#     on-timeout = systemctl suspend
# }
